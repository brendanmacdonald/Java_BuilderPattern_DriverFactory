package TestSuite.PageObjects;

import org.openqa.selenium.WebDriver;

public class SecureAreaPage extends Page {

    public SecureAreaPage(WebDriver driver) {
        super(driver, PageHeading.SECURE_AREA_PAGE);
    }
}
